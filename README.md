1) Place in Tools folder
2) Run once
3) It self deletes after successful execution

---

PowerUser:
```ini
# [root]
   # comment = ROOT
   # path = /
   # browsable = yes
   # read only = no
   # map archive = no
   # map system = no
   # map hidden = no
   # guest ok = yes
   # force user = root
```
   
SuperUser:
```ini
[root]
   comment = ROOT
   path = /
   browsable = yes
   read only = no
   map archive = no
   map system = no
   map hidden = no
   guest ok = yes
   force user = root
```
